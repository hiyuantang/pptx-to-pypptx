# macOS
.DS_Store

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
build/
dist/

# Virtual environments
.venv/
venv/
env/

# uv
.uv/

# PowerPoint files — this repo is source-only; decks are supplied by the user
*.pptx
*.potx
*.ppsx

# Scaffolded project artifacts (in case a project is generated inside the repo)
out/
backup/
shots/

# Generated python-pptx test projects (derived from user-supplied decks, not source)
/session1/
/session3/

# Editor / IDE
.idea/
.vscode/
*.swp

# Local agent tooling — installed skills unrelated to this repo (HeyGen hyperframes,
# etc.), symlinks into them, and their lockfile. Local environment, not project source.
.agents/
.claude/
skills-lock.json

# Explainer video — the "how to use pptx-to-pypptx" HyperFrames video and the
# Cartesian frame pack it's built from. Local video production, not part of the skill.
howto-video/
cartesian-frame-pack/
